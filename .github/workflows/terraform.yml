name: Gunluk Ilan Tarayici

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    steps:
      - name: Dosyalari Al
        uses: actions/checkout@v4

      - name: Python Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Gerekli Araclari Yukle
        run: pip install ddgs requests beautifulsoup4

      - name: Ilanlari Tara
        run: python scraper.py

      - name: Yeni Ilanlari Kaydet
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          git add ilanlar.json
          git diff --staged --quiet && echo "Degisiklik yok" && exit 0
          git commit -m "Otomatik Guncelleme: Yeni ilanlar eklendi"
          git push
